# TagForge 数据审核记录 — 2026.07.25

## 审核基线

- 协议修订日期：`2026-07-25`
- 更新前数据日期：`2026.07.3`
- 更新后数据日期：`2026.07.25`
- 更新类型：纯格式与开发期身份调整
- 官方身份：`tagforge-official`
- 公开名称：`TagForge 官方数据集` / `TagForge Official Dataset`

## 范围与来源状态

本次没有抓取、添加、删除、翻译或语义修改外部内容。既有来源快照和逐条 Prompt 审核记录继续有效；`data-cache/` 未进入提交。

本次只改变：

- 官方 Pack 身份、名称、数据更新日期和静态资源路径
- Data Pack Draft、结果快照、本地备份和分析 Manifest 的字段结构
- Prompt 来源标识：`tagforge-original`
- IndexedDB 开发期库名、安装包唯一键和当前分享结构
- 构建、验证、模拟、分析、界面、测试与活跃文档

## 内容守恒

确定性脚本更新五个 canonical 数据文件，并对更新前提交逐项比较：

- 9 个 Category 的 ID、文本、颜色和启用状态不变
- 427 个 Entry 的 ID、文本、分类、Family、Facet、权重、风险与引用不变
- 1000 个原创 Prompt 的 ID、文本、类型、Family、Motif、权重与启用状态不变
- 34 个历史 Prompt 的全部内容不变
- 5 个 Recipe 的槽位、Variant、冷却与风险策略不变
- 424 个有效 Entry、3 个 `deprecatedBy` 记录和 1034 个有效 Prompt 数量不变
- 唯一批量字段变化为 `dataVersion` 和原创 Prompt 的稳定来源标识

没有进行 AI 语义决策。

## Draft 与运行时调整

- 删除 Pack、结果、备份和分析中的格式/分析器版本字段
- `PackRef` 使用 `packId + dataVersion + checksum`
- 已安装 Pack 以 `packId` 为唯一键，同 ID 内容覆盖
- 历史与收藏继续按 checksum 隔离
- IndexedDB 使用 `tagforge-dev`，不迁移旧浏览器数据
- 分享链接只接受当前 Fragment 快照结构
- 删除旧 query、本地存储迁移、迁移标记表和一次性迁移工具
- 官方产物固定为：
  - `packs/tagforge-official.tagforge.json`
  - `packs/official-registry.json`
  - `analysis/tagforge-official/`
- 构建前清理受管理的 `.tmp/public/`，没有残留旧官方资源路径

## Checksum 与分析

- 更新前 checksum：`sha256:dcee21564a68b7394063fd11a99e23a71a75ac4b623dc5f3b9c956d8bbe7778b`
- 更新后 checksum：`sha256:069ca7eb678718840eb5a8a610113017b3290ccc26e64c7630434e65eb00fb76`
- 分析节点：424
- 分析边：3114
- Louvain 社区：4
- `analysis:build` 与 `analysis:verify` 字节级一致
- 注册表、Pack 与分析 Manifest 的 `packId`、`dataVersion` 和 checksum 一致

## 模拟结果

每个 Recipe 运行 2000 次：

- 无非法 Entry 组合
- 无近期精确词对重复
- 声明可达 Entry：424 / 424
- 声明可达 Prompt：1034 / 1034
- 抽样覆盖 Entry：424 / 424
- 抽样覆盖 Prompt：887 / 1034

抽样未命中的 Prompt 属于有限随机样本，不影响静态可达性；完整声明可达性已由验证器确认。

## 完整验证

`pnpm data:verify` 通过：

- Pack 构建与当前 Draft 校验通过
- Prompt 来源相似度通过：1000 个 Prompt、153 条参考
- 五个 Recipe 模拟通过
- 分析构建与确定性验证通过
- TypeScript 类型检查通过
- Vitest：4 个测试文件、21 个测试通过
- Vite 生产构建通过
- Playwright：7 个端到端测试通过

附加检查：

- Category、Entry、Prompt 和 Recipe 内容守恒检查通过
- 当前源码和活跃文档中的旧版本化身份扫描为 0；历史审核记录未重写
- 生成产物中不存在旧官方 Pack 或分析目录
- `git diff --check` 在提交前执行

## 已知限制

项目仍处于单人开发阶段。Data Pack Draft、结果快照、本地备份、IndexedDB 和资源路径不承诺向后兼容；旧站点数据需要在实际浏览器中清除后从当前开发态重新开始。
